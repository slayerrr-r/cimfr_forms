{
  "name": "lab-forms",
  "version": "1.0.0",
  "private": true,
  "description": "CIMFR Laboratory Information Management System",

  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && python -m uvicorn app.main:app --reload",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },

  "repository": {
    "type": "git",
    "url": "git+https://github.com/slayerrr-r/cimfr_forms.git"
  },

  "author": "",
  "license": "ISC",

  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}